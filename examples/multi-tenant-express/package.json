{
  "name": "ai-sdk-rate-limiter-multi-tenant-express",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-tenant Express API with per-user rate limiting using ai-sdk-rate-limiter",
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc"
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.3.0",
    "ai": "^4.3.0",
    "ai-sdk-rate-limiter": "../../",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
